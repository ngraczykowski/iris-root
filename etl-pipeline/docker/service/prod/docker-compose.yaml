version: '2.3'

services:
  etl_pipeline_service:
    image: docker.repo.silenteight.com/etl-pipeline-service
    volumes:
      - /home/artur/repos/silenteight/etl-pipeline/etl_pipeline/config:/config:rw
    ports:
      - "9090:9090" # jupyter notebook forward
    environment:
      - CONFIG_APP_DIR=/config
