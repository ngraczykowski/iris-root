@Library('pipeline-shared-library') _

pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh './scripts/build.sh'
            }
        }
        stage('Push') {
            when {
                branch 'master'
            }
            steps {
                sh './scripts/push.sh'
            }
        }
    }
}
