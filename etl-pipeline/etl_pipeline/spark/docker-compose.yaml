version: '2.3'

services:
  spark_playground:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    volumes:
      - .:/app
    ports:
      - "8888:8888" # jupyter notebook forward
      - "6006:6006" # tensorboard forward
      - "4040:4040"