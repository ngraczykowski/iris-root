DATA_SOURCE_INPUT_ENDPOINT: {{ range $index, $client := service (printf "%s-grpc-universal-data-source" (env "NOMAD_NAMESPACE") ) -}}
          {{ if $index }},{{ end -}}
          {{ $client.Address }}:{{ $client.Port -}}
      {{- end }}
ETL_SERVICE_CONCATENATED_ENDPOINT: {{ env "NOMAD_ADDR_grpc" }}
ETL_SERVICE_PORT: {{ env "NOMAD_ADDR_grpc" }}
ETL_SERVICE_HOSTNAME: {{ env "NOMAD_ADDR_grpc" }}

