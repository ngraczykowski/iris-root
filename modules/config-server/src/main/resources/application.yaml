server:
  port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          order: 1
          searchLocations:
            - classpath:/configurations
            - classpath:/configurations/{application}
