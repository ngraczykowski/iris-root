####################################################################################################
# Defaults (overridden by environment variables or arguments)
####################################################################################################
simulator.messaging.concurrency=2
simulator.messaging.prefetch=10

####################################################################################################
# Configuration
####################################################################################################
spring.rabbitmq.listener.direct.consumers-per-queue=${simulator.messaging.concurrency}
spring.rabbitmq.listener.direct.prefetch=${simulator.messaging.prefetch}
spring.rabbitmq.listener.simple.max-concurrency=${simulator.messaging.concurrency}
spring.rabbitmq.listener.simple.prefetch=${simulator.messaging.prefetch}
