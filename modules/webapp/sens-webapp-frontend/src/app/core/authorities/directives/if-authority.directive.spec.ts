import { IfAuthorityDirective } from './if-authority.directive';

describe('IfAuthorityDirective', () => {
  it('should create an instance', () => {
    const directive = new IfAuthorityDirective();
    expect(directive).toBeTruthy();
  });
});
