import { FormFieldEmptyControlDirective } from './form-field-empty-control.directive';

describe('FormFieldEmptyControlDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldEmptyControlDirective();
    expect(directive).toBeTruthy();
  });
});
