import { FxDirective } from './fx.directive';

describe('FxDirective', () => {
  it('should create an instance', () => {
    const directive = new FxDirective();
    expect(directive).toBeTruthy();
  });
});
