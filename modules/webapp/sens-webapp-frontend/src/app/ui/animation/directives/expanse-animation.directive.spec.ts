import { ExpanseAnimationDirective } from './expanse-animation.directive';

describe('ExpanseAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpanseAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
