includeBuild('../../platforms')
includeBuild('../../build-logic')

dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files("../../gradle/libs.versions.toml"))
    }
  }
}

rootProject.name = 'rabbitmq-common-schema'

// NOTE(ahaczewski): Keep sorted alphabetically.
include "sear-rabbitmq-common-schema-definitions"
include "sear-rabbitmq-common-schema-initializr-app"
