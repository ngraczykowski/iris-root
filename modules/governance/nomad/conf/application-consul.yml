grpc:
  client:
    governance:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-governance

    reco:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-reco

    pipeline:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-pipeline

    gateway:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-gateway

    name-agent:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-name-agent

    document-number-agent:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-document-number-agent

    document-comparer-agent:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-document-comparer-agent

    gender-agent:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-gender-agent

    country-agent:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-country-agent

    date-agent:
      address: discovery:///{{ env "NOMAD_NAMESPACE" }}-grpc-date-agent
