####################################################################################################
# Moved from serp-governance module - don't know if required so delete if not
####################################################################################################

# none

####################################################################################################
# CONFIGURATION
####################################################################################################

# Spring Batch
spring:
  batch:
    # Batch schema management is done with `db/changelog/db.changelog-batch.xml`.
    initialize-schema: never
    job:
      enabled: false
    table-prefix: ${serp.db.schema}.batch_
