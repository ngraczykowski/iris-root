apiVersion: v1
kind: Service
metadata:
  name: sear-payments-bridge-service
spec:
  selector:
    app: {{ .Values.name }}
  ports:
    - protocol: TCP
      name: http
      port: {{ .Values.ports.http }}
      targetPort: http
    - protocol: TCP
      name: grpc
      port: {{ .Values.ports.grpc }}
      targetPort: grpc
