#---------------------------------------------------------------------------------------------------
# SERVER
#---------------------------------------------------------------------------------------------------

server:
  forward-headers-strategy: native
  undertow:
    threads:
      io: 4
      worker: 16

  servlet:
    context-path: /rest/${spring.application.name}

  error:
    include-exception: true
    include-stacktrace: never

#---------------------------------------------------------------------------------------------------
# MANAGEMENT
#---------------------------------------------------------------------------------------------------

management:
  endpoint:
    health.show-details: always

  endpoints:
    web:
      base-path: /management
      exposure:
        include: [ "health", "info", "metrics", "prometheus", "loggers" ]
    # loggers endpoint is secured with mTLS
    loggers:
      sensitive: false

  metrics:
    tags:
      application: ${spring.application.name}

warehouse:
  web:
    cors:
      allowed-origins-patterns: "*"
      allowed-methods: "*"
      allowed-headers: "*"
      exposed-headers: "Authorization,Link,X-Total-Count"
      allow-credentials: true
      max-age: 1800
  messaging:
    concurrency: 4
    prefetch: 25
  kibana:
    timezone: Europe/Warsaw
  report:
    default-bucket: reports
    synchronization:
      production-tenant: ${warehouse.environment.prefix}_production_periodic
      cron: "-"
    removal:
      cron: "0 0 1 * * ?"
      duration-time: P1D
    simulation:
      master-tenant: ${warehouse.environment.prefix}_simulation_master
  opendistro:
    max-object-count: 1000
  es:
    production-query-index: ${warehouse.environment.prefix}_production
    country-roles-indexes:
      - ${warehouse.es.production-query-index}
      - ${warehouse.environment.prefix}_simulation*
  data:
    production:
      status:
        field-name: alert_status
        completed-value: COMPLETED

spring:
  #-------------------------------------------------------------------------------------------------
  # GENERAL
  #-------------------------------------------------------------------------------------------------
  aop:
    auto: false

  task:
    execution:
      pool:
        core-size: 8
        keep-alive: 10s
        max-size: 16
        queue-capacity: 10000
      thread-name-prefix: pool-task-

    scheduling:
      pool:
        size: 2
      thread-name-prefix: pool-scheduling-

  main:
    banner-mode: 'off'

  #-------------------------------------------------------------------------------------------------
  # MESSAGING
  #-------------------------------------------------------------------------------------------------
  rabbitmq:
    ssl:
      enabled: false
    listener:
      type: simple
      direct:
        consumers-per-queue: ${warehouse.messaging.concurrency}
        prefetch: ${warehouse.messaging.prefetch}
      simple:
        concurrency: 1
        max-concurrency: ${warehouse.messaging.concurrency}
        prefetch: ${warehouse.messaging.prefetch}


  #-------------------------------------------------------------------------------------------------
  # DATABASE
  #-------------------------------------------------------------------------------------------------
  datasource:
    # Postgres database
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://${warehouse.db.host}:${warehouse.db.port}/${warehouse.db.name}?currentSchema=${warehouse.db.schema:public}
    username: ${warehouse.db.username}
    password: ${warehouse.db.password}
    initialization-mode: never

    # Hikari pool configuration
    hikari:
      pool-name: Database-Pool
      minimum-idle: ${warehouse.db.connections.min:1}
      maximum-pool-size: ${warehouse.db.connections.max:10}
      auto-commit: false
      schema: ${warehouse.db.schema:public}
      isolate-internal-queries: true
      # NOTE(ahaczewski): Forces HikariCP to commit the transaction after opening fresh connection.
      #  Without it, the connection transaction is left in OPEN state instead of IDLE, because
      #  just before HikariCP runs `SET SESSION search_path TO '${ae.db.schema}'` which begins
      #  transaction. Thus, with that query as workaround, we force the transaction commit.
      connection-init-sql: SELECT 1
      data-source-properties:
        ApplicationName: Warehouse

  # Database migrations
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    default-schema: ${warehouse.db.schema:public}
    contexts: default

  # Spring Data JPA
  jpa:
    database: POSTGRESQL
    show-sql: false
    open-in-view: false
    database-platform: com.silenteight.sep.base.common.support.hibernate.FixedPostgreSql95Dialect

    hibernate:
      ddl-auto: none
      use-new-id-generator-mappings: true

    properties:
      hibernate.cache.use_query_cache: false
      hibernate.connection.provider_disables_autocommit: true
      hibernate.generate_statistics: false
      hibernate.jdbc.lob.non_contextual_creation: true
      hibernate.jdbc.time_zone: UTC
      hibernate.temp.use_jdbc_metadata_defaults: false
      org.hibernate.envers.audit_table_suffix: _audit
      org.hibernate.envers.store_data_at_delete: true

  #---------------------------------------------------------------------------------------------------
  # CONSUL
  #----------------------------------------------------------------------------------------------------

  cloud:
    discovery:
      enabled: false
    consul:
      enabled: false
    config:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false

#---------------------------------------------------------------------------------------------------
# SEP BASE CONFIGURATION
#---------------------------------------------------------------------------------------------------

serp:
  show-env: true

  messaging:
    error-queue:
      enabled: true


#-------------------------------------------------------------------------------------------------
# REST API AUTH
#-------------------------------------------------------------------------------------------------

sep:
  auth:
    permissionMethodsMappings:
      DOWNLOAD_SIMULATION_REPORTS:
        - "DOWNLOAD_SIMULATION_REPORT"
        - "GET_SIMULATION_TENANT"
        - "CREATE_SIMULATION_REPORT"
      DOWNLOAD_SOLVING_REPORTS:
        - "DOWNLOAD_PRODUCTION_ON_DEMAND_REPORT"
        - "LIST_PRODUCTION_ON_DEMAND_REPORTS"
        - "CREATE_PRODUCTION_ON_DEMAND_REPORT"
      DOWNLOAD_PERIODIC_REPORTS:
        - "SYNCHRONIZE_REPORTS"
      VIEW_USERS:
        - "GET_COUNTRY_GROUP"
        - "LIST_COUNTRY_GROUP"
        - "GET_COUNTRIES"
      MANAGE_USERS:
        - "CREATE_COUNTRY_GROUP"
        - "UPDATE_COUNTRY_GROUP"
        - "UPDATE_COUNTRIES"
        - "DELETE_COUNTRY_GROUP"
      VIEW_QA_ALERTS:
        - "VIEW_ALERTS_DATA"
      MANAGE_QA_ALERT:
        - "VIEW_ALERTS_DATA"
      VIEW_QA_VERIFICATION:
        - "VIEW_ALERTS_DATA"
      MANAGE_QA_VERIFICATION:
        - "VIEW_ALERTS_DATA"
      DASHBOARD:
        - "VIEW_ALERTS_DATA"
      VIEW_SIMULATION_RESULT:
        - "LIST_SIMULATION_REPORTS"

    rolePermissionsMappings:
      USER_ADMINISTRATOR:
        - "DOWNLOAD_AUDIT_REPORTS"
        - "DOWNLOAD_USERS_REPORTS"
        - "MANAGE_USERS"
        - "VIEW_USERS"
      MODEL_TUNER:
        - "VIEW_SIMULATION_RESULT"
        - "DOWNLOAD_SIMULATION_REPORTS"
        - "DOWNLOAD_SOLVING_REPORTS"
        - "DASHBOARD"
      APPROVER:
        - "VIEW_SIMULATION_RESULT"
        - "DOWNLOAD_SIMULATION_REPORTS"
        - "DOWNLOAD_SOLVING_REPORTS"
        - "DASHBOARD"
      AUDITOR:
        - "VIEW_SIMULATION_RESULT"
        - "VIEW_QA_ALERTS"
        - "DASHBOARD"
      QA:
        - "VIEW_QA_ALERTS"
        - "MANAGE_QA_ALERT"
        - "VIEW_QA_VERIFICATION"
        - "DASHBOARD"
      QA_ISSUE_MANAGER:
        - "VIEW_QA_VERIFICATION"
        - "MANAGE_QA_VERIFICATION"
        - "DASHBOARD"

warehouse.report.rbs:
  production:
    columns:
      - name: fvSignature
        label: fvSignature
      - name: policy_id
        label: policy_id
      - name: step_id
        label: step_id
      - name: recommended action
        label: recommended action
      - name: categories/customerType
        label: categories/customerType
      - name: features/name
        label: features/name
    grouping-columns:
      - name: qa_decision
        label: qa_decision
        grouping-values: [PASS, FAILED]
        add-counter: true
      - name: analysis_decision
        label: analysis_decision
        grouping-values: FP, PTP
        add-counter: false
    date-field-name: alert_timestamp

  simulation:
    columns:
      - name: fvSignature
        label: fvSignature
      - name: policy_id
        label: policy_id
      - name: step_id
        label: step_id
      - name: recommended action
        label: recommended action
      - name: "categories/customerType"
        label: "categories/customerType"
      - name: features/name
        label: features/name
    grouping-columns:
      - name: qa_decision
        label: qa_decision
        grouping-values: [PASS, FAILED]
        add-counter: true
      - name: analysis_decision
        label: analysis_decision
        grouping-values: FP, PTP
        add-counter: false
    date-field-name: alert_timestamp

warehouse.report.sm:
  risk-type:
    name: riskType
    label: Risk Type
  country:
    name: country
    label: Country
  recommendation-field:
    name: recommended action
    decision-values: [FALSE_POSITIVE, POTENTIAL_TRUE_POSITIVE]
  date-field-name: alert_timestamp

warehouse.report.statistics:
  ai-decision:
    field: alert_recommendation_recommended_action
    significant-values: [ACTION_FALSE_POSITIVE, ACTION_POTENTIAL_TRUE_POSITIVE]
    false-positive-value: ACTION_FALSE_POSITIVE
  analyst-decision:
    field: alert_DN_CASE.currentState
    false-positive-value: Level 1 Review
  date-field-name: index_timestamp
