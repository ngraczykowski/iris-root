server:
  port: 24960

test:
  hsbc-bridge-client:
#   Every 60 seconds
    cron: "*/60 * * * * ?"
    batch-prefix: datagen
    batch-size: 1000
    base-url: https://bravo.dev.silenteight.com/rest/hsbc-bridge/
    ingest:
      alert-template: classpath:alert_ingest.tmpl.json
      batch-template: classpath:batch_ingest.tmpl.json
    learning:
      alert-template: classpath:alert_learning.tmpl.json
