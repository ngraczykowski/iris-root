apiVersion: v1
kind: Service
metadata:
  name: warehouse-service
spec:
  selector:
    app: {{ .Values.name }}
  type: NodePort
  ports:
    - protocol: TCP
      name: http
      port: {{ .Values.ports.http }}
      targetPort: {{ .Values.ports.http }}

