#-------------------------------------------------------------------------------------
# KEYCLOAK
#-------------------------------------------------------------------------------------------------
keycloak:
  adapter:
    authServerUrl: fakeAuthHost:8000
    realm: sens-webapp
    publicClient: false
    confidentialPort: 0
    principalAttribute: preferred_username
    resource: backend
    sslRequired: external
    credentials.secret: 55555555-4444-3333-2222-000111000111


warehouse:
  messaging:
    integration:
      alert-indexing-inbound:
        queue-name: warehouse.index-request
      alert-indexed-outbound:
        exchange-name: alert-indexed
        routing-key: alert.indexed
      alert-indexing-test-client-outbound:
        exchange-name: alert-indexing
        routing-key: alert.new
      alert-indexed-event-test-listener-inbound:
        queue-name: client.index-response
