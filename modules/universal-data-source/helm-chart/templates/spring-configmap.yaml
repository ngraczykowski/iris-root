apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-spring
data:
  application.yml: |
    management:
      server:
        port: {{ .Values.ports.management }}
    uds:
      category:
        available:
          {{- range .Values.category.available }}
            - {{ . | quote }}
          {{- end }}
