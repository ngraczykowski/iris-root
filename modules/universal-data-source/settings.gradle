includeBuild('../../platforms')
includeBuild('../../build-logic')

dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files("../../gradle/libs.versions.toml"))
    }
  }
}

rootProject.name = 'universal-data-source'
gradle.ext.sepDependenciesVersion = "2.5.0"

include 'universal-data-source-app'
include 'universal-data-source-common'
include 'universal-data-source-dist'
