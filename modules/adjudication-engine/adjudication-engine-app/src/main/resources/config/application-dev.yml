# Spring `dev` profile configuration
serp:
  show-env: true
  messaging:
    error-queue:
      declare: false
      enabled: true
      name: adjudication-engine.error-queue

logging:
  level:
    com.silenteight.sep: DEBUG
    com.silenteight.adjudication.engine: DEBUG
    io.opencensus: INFO
    io.netty: INFO
    io.grpc.netty: INFO
    org.quartz.core.JobRunShell: DEBUG

management:
  info:
    git:
      mode: full

  endpoints:
    enabled-by-default: true

    web:
      exposure:
        include: "*"

server:
  error:
    include-stacktrace: on_trace_param

spring:
  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate.generate_statistics: true

  jackson:
    serialization:
      indent-output: true
