apiVersion: v1
kind: Service
metadata:
  name: adjudication-engine-service
spec:
  selector:
    app: {{ .Values.name }}
  ports:
    - port: {{ .Values.ports.http }}
      name: http
      targetPort: http
    - port: {{ .Values.ports.grpc }}
      name: grpc
      targetPort: grpc
