logging.level.com.silenteight: DEBUG

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

dataprep:
  rabbitmq:
    retry:
      initial-interval: 100
      multiplier: 1