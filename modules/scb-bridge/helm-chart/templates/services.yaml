apiVersion: v1
kind: Service
metadata:
  name: scb-bridge-app-service
spec:
  selector:
    app: {{ .Values.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.ports.http }}
      name: http
      targetPort: http
