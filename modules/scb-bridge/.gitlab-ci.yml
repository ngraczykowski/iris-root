stages:
  - build
  - test
  
cache:
  paths:
    - .gradle/wrapper
    - .gradle/caches

build:
  stage: build
  when: manual
  script:
    - ./gradlew assemble

test:
  stage: test
  when: manual  
  script:
    - ./gradlew check