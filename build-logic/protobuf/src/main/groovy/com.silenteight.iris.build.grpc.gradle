plugins {
  id "com.silenteight.iris.build.protobuf"
}

protobuf {

  generateProtoTasks {
    all()*.plugins {
      grpc {}
    }
  }

  plugins {
    grpc {
      artifact = libs.grpc.gen.get()
    }
  }
}
