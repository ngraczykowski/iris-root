includeBuild('../platforms')
includeBuild('../build-logic')

dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files("../gradle/libs.versions.toml"))
    }
  }
}

rootProject.name = 'modules-tm'

includeBuild('transaction-monitoring')
