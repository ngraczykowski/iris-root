dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files("../gradle/libs.versions.toml"))
    }
  }
}

rootProject.name = 'iris-platforms'

include('product-platform')
include('test-platform')
include('plugins-platform')
