plugins {
  id('java-platform')
}

group = 'com.silenteight.iris.platform'

dependencies {
  constraints {
    api(libs.plugin.cyclonedx.bom)
    api(libs.plugin.jib)
    api(libs.plugin.nebula.resolution)
    api(libs.plugin.sonarqube)
    api(libs.plugin.spring.boot)
    api(libs.plugin.freefair.aspectj)
    api(libs.plugin.freefair.aspectj.weaving)
    api(libs.plugin.git.properties)
    api(libs.plugin.google.protobuf)
  }
}
