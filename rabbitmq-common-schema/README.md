# RabbitMQ Common Schema

This project holds a library with RabbitMQ schema definition of common broker infrastructure (exchanges, queues, etc.).

The initializr is an application that simply pulls in the library, connects to RabbitMQ, and applies the common schema.